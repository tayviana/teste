import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'VIVAFIT', // <--- Mudei para VIVAFIT (Maiúsculo)
  webDir: 'www'
};

export default config;